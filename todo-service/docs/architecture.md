```mermaid
timeline
    title Todo Application Architecture
    section Backend
      API Server  : Spring Boot (8081)
      Database    : H2 (in‑memory)
    section Frontend
      Angular SPA : Hosted at localhost:4200
      Components : TodoListComponent, TodoService
```
